{% extends 'base-back.html.twig' %}

{% block title %}Departement{% endblock %}

{% block body %}
    <h1>Departement</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ departement.id }}</td>
            </tr> #}
            <tr>
                <th>Nomdepartement</th>
                <td>{{ departement.nomdepartement }}</td>
            </tr>
            <tr>
                <th>Zonedepartement</th>
                <td>{{ departement.zonedepartement }}</td>
            </tr>
            <tr>
                <th>Detaildepartement</th>
                <td>{{ departement.detaildepartement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_departement_index') }}">back to list</a>

    <a type="button" class="btn btn-block bg-gradient-warning" href="{{ path('app_departement_edit', {'id': departement.id}) }}">edit</a>

    {{ include('departement/_delete_form.html.twig') }}
{% endblock %}
