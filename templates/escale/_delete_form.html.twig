<form method="post" action="{{ path('app_escale_delete', {'idEscale': escale.idEscale}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ escale.idEscale) }}">
    <button class="btn btn-block btn-outline-danger btn-lg my-1">Supprimer</button>
</form>
