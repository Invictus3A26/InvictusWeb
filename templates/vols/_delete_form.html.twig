<form method="post" action="{{ path('app_vols_delete', {'idVol': vol.idVol}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vol.idVol) }}">
    <button class="btn btn-block btn-outline-danger btn-lg my-1">Delete</button>
</form>
