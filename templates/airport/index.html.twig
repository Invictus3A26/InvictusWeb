{% extends 'base.html.twig' %}

{% block title %}Airport index{% endblock %}

{% block body %}
<div class="container-fluid mt-5">
<div class="row">
<div class="col-12">
 <div class="card card-info" >
              <div class="card-header">
                <h3 class="card-title">Gestion Aeroport</h3>

                <div class="card-tools">
                  <div class="input-group input-group-sm" style="width : 200px;">
                    <input type="text" name="table_search" class="form-control float-right" placeholder="Search">

                    <div class="input-group-append">
                      <button type="submit" class="btn btn-default">
                        <i class="fas fa-search"></i>
                      </button>
                    </div>
                  </div>
                </div>
              </div>
              <!-- /.card-header -->
              <div class="card-body table-responsive p-0">
                <table class="table table-hover text-nowrap">
        <thead>
            <tr>
            
                <th>NomAeroport</th>
                <th>VilleAeroport</th>
                <th>Pays</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for airport in airports %}
            <tr>
            
                <td>{{ airport.nomAeroport }}</td>
                <td>{{ airport.villeAeroport }}</td>
                <td>{{ airport.pays }}</td>
                <td>
                    <a href="{{ path('app_airport_show', {'idAeroport': airport.idAeroport}) }}"><i class="fa-solid fa-circle-info"></i></a>
                    <a href="{{ path('app_airport_edit', {'idAeroport': airport.idAeroport}) }}"><i class="fa-solid fa-screwdriver-wrench"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
     <a type="button" class="btn btn-block btn-outline-success btn-xs my-1" href="{{ path('app_airport_new') }}"  style="width : 200px; margin : auto; ">Ajoute un aèroport</a>
     </div>
     
    </div>
  </div>
  </div>
  </div>
            <script src="//code.jquery.com/jquery.js"></script>
 
    <!-- Load Flashy default JavaScript -->
    <script src="{{ asset('bundles/mercuryseriesflashy/js/flashy.js') }}"></script>
              {{ include('@MercurySeriesFlashy/flashy.html.twig') }}

   
{% endblock %}
