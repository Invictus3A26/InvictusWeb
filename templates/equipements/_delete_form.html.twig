<form method="post" action="{{ path('app_equipements_delete', {'id': equipement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipement.id) }}">
    <button type="submit" class="btn btn-block btn-outline-danger btn-sm">Delete</button>
</form>
